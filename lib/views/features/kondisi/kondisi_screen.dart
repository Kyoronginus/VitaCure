import 'package:flutter/material.dart';

class KondisiScreen extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('Kondisi Screen'),
      ),
      body: Center(
        child: Text('Ini adalah layar sementara untuk Kondisi Screen'),
      ),
    );
  }
}
